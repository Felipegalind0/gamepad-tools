# Gamepad Tools assets

No external controller assets are committed.

Current assets:
- None. The optional viewer (`src/viewer/index.ts`) builds a generic controller in code from
  Babylon.js primitives (boxes, spheres and cylinders) with plain colored materials. It loads no
  model, texture or font files.

When controller models are added, list provenance, authorship, licenses, and local modifications here.
